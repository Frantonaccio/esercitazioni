# GAP MATRIX — PRE-FIX (Phase A: RECEIVE / INSPECT / REPRODUCE)

> **v2**: questa matrice descrive lo stato PRE-FIX della prima consegna e resta immutata come evidenza storica.
> Il delta correttivo richiesto da Human Review 01 (blocker NG-03) e' documentato in `CORRECTIVE_DELTA_NG03.md`
> con la propria riproduzione (`evidence/pre_fix/reproduction_ng03.json`) e le proprie controprove (B12).

Baseline: Runtime `39c82968` · Core `740ee979` · P2 `637f3a80…` frozen. Riproduzioni eseguite sul codice NON modificato:
`evidence/pre_fix/reproduction_baseline.json` (script: `pbgate/reproduce_pre_fix.py`). Nessun provider, nessuna credenziale, 0 crediti.

Stati ammessi: REPRODUCED · NOT_REPRODUCED · BLOCKED_ENVIRONMENT · ALREADY_IMPLEMENTED · STILL_OPEN.

| Gap | File / funzione reali | Prova di riproduzione (pre-fix) | Rischio | Fix minimo (Phase B) | Test richiesto (Phase C) | Stato pre-fix |
|---|---|---|---|---|---|---|
| **P-B01** separazione di privilegio orchestrator / spender | `tests/boundary_mock.py:worker_main` (worker = `multiprocessing` child, stesso UID); `tests/run_gate.py:t29` (4 probe); `tests/gate_report.py:t29_status` | Evidenza storica `evidence/T29_boundary_privilege_isolation.json`: `read_worker_secret / write_worker_code / write_worker_store / direct_dispatch_on_worker_store = BYPASS_POSSIBLE`, uid 0/0 → `BLOCKED_ENVIRONMENT`. Riconfermato nella regressione R0-R1 di questa fase. | L'orchestrator puo' leggere il segreto, riscrivere il worker, scrivere lo store, fare dispatch diretto: nessun confine, solo un `if`. | NON si tocca il mock storico. Si aggiunge un **daemon spender** (processo con **UID distinto**, secret dir e store `0700` del suo UID, socket Unix con **SO_PEERCRED**, peer UID autorizzato unico) e un **orchestrator probe** con UID distinto, **senza capability**, `no_new_privs`. Il gate rileva se l'ambiente puo' cambiare UID (root + `setpriv` + CAP_SETUID): se no → `BLOCKED_ENVIRONMENT`, mai un PASS simulato. | B01 (4 probe canoniche via `gate_report.t29_status(same_uid=False, …)` + probe IPC peer-uid, kill, /proc), B02 (controllo same-uid resta BLOCKED). | **REPRODUCED** (same-UID) — ambiente corrente **idoneo** a una dimostrazione reale (verificato: `setpriv --reuid`, DAC su 0600/0700, `SO_PEERCRED` cross-uid, `no_new_privs` blocca `sudo`). |
| **P-B02** reconciliation autenticata e legata all'identita' | Core `registry/reservations.py:SqliteReservationStore.reconcile` (accetta qualunque mapping con `source`+`remote_ref` non vuoti; `adapters/base.py:validate_reconciliation_evidence`); nel Runtime **nessun** chiamante di `reconcile` (`runtime/*.py`: 0 occorrenze); `tests/worker.py:store_call` puo' invocare `reconcile` con evidenza arbitraria. | `reproduction_baseline.json["P-B02"]`: job `SUBMIT_UNKNOWN` di `fake/acct_owner`; `reconcile` con evidenza che nomina provider `higgsfield`, conto `acct_stranger`, `pv_other`, token `att_bogus` → **accettato** (`SUCCEEDED`); la **stessa** evidenza riusata su un secondo job di un'altra operazione → **accettata** (`FAILED`). | Una risposta non autenticata, di un altro conto/provider/job, o riproposta, chiude un'incertezza economica (libera l'identita' della spec) senza prova. | Modulo Runtime `runtime/reconciliation.py` (nessuna modifica Core): report firmato **HMAC** con chiave derivata dal segreto dello spender (equivalente LAB dell'interlocutore autenticato), binding a `provider / provider_account / provider_job_id / attempt_token / payload_digest / operation_id / job_id`, ownership dell'adapter, stato riconciliabile, **nonce monouso persistito** (O_EXCL), audit dello snapshot P-B04; solo poi `store.reconcile` (percorso Core esplicito). | B03 (happy path + 7 controprove obbligatorie fail-closed), B04 (replay, cross-operation, response valida di altra operazione). | **REPRODUCED** |
| **P-B04** snapshot exact-byte immutabile del payload autorizzato | Core `transport/pipeline.py:run_job` (persiste solo `sha256` in `mark_submitting`, riga `reservations.payload_digest`); `adapters/fake.py:MockTransport.authorize/send` (set di digest **in memoria**, stesso dominio di privilegio); Runtime `runtime/go_candidate.py:go` (nessuno snapshot). | `reproduction_baseline.json["P-B04"]`: dopo un `go` riuscito i byte canonici **non esistono su disco** (`authorized_bytes_persisted_under_state: []`); adapter che autorizza da solo un secondo digest e invia byte diversi: il **marcatore di invio viene raggiunto** (`transport_sent_count: 1`) e il mismatch e' rilevato solo **dopo** l'invio (`PAYLOAD_DIGEST_MISMATCH`, job `SUBMIT_UNKNOWN`). | Nessuna prova che i byte autorizzati siano quelli consegnati; nessuna verifica su evidenza persistita immediatamente prima dell'invio; nessuna verifica in audit/reconciliation. | Modulo Runtime `runtime/payload_snapshot.py`: ledger **write-once** (O_EXCL, `0444`, content-addressed) dei byte canonici sotto `state/<store>.snapshots/`, sigillo legato a `operation_id/spec_key/provider/provider_account`, binding all'attempt (`job_id/attempt_token`) al momento di `mark_submitting`, **guard sul trasporto**: immediatamente prima del `send` rilegge i byte persistiti, confronta **byte per byte**, verifica digest e binding, attesta l'invio (monouso per attempt). Rifiuto **prima** del marcatore → il Core lo tratta come `REFUSED_BEFORE_SEND` (costo attestato 0). Audit in reconciliation. | B05 (bytes persistiti, digest, immutabilita', binding, attestazione pre-send), B06 (nested mutation, serialization drift, field reorder, payload sostituito dopo authorization, snapshot mancante, digest mismatch, snapshot di altra operazione). | **REPRODUCED** |
| **LEGACY SPEND PATHS** | `runtime/go_candidate.py:go` (`authorization=None` → `LEGACY_LAB`: budget grezzo, nessun envelope/quote/permit, resume-as-start); `runtime/hf_batch_runtime.py:Batch.run_job` (LEGACY_LAB se `authorization=None`), `Batch.lock/quote` (`_real_cli()`), `__main__`; `tests/worker.py:run_go/store_call` (dispatch diretto, stesso UID); P2 `hf_batch.py` frozen (CLI reale, fuori runtime); Core `transport.pipeline.run_job` / `adapter.submit` (primitive raggiungibili da chiunque abbia il Core in `sys.path`). | `reproduction_baseline.json["LEGACY_SPEND_PATH"]`: `go(authorization=None)` con `intent=resume` su operazione vuota → **submit** riuscito, `budget_units=0`, envelope/quote/permit `None`, ledger `RESERVE` con envelope NULL. | Un percorso spendibile che aggira authorization/quote/envelope/permit; oggi solo `FakeAdapter` (fail-closed su provider reale), ma resterebbe aperto al futuro Provider Boundary Gate. | Inventario completo (`LEGACY_SPEND_PATHS.md`) + **interruttore fail-closed** `runtime/provider_gate.py:LEGACY_LAB_SPEND_PATH` verificato in `go()` prima di qualunque effetto (`LEGACY_SPEND_PATH_DISABLED`); chiudibile via env (`CREATIVE_OS_LEGACY_LAB_SPEND_PATH=disabled`, solo in chiusura) o costante. Non eliminabile in questa fase: T01-T27 storici lo usano → `BLOCKED_PROVIDER_GATE`. | B07 (inventario statico + dinamico: switch chiuso → rifiuto a zero effetti, governato invariato; `hf_batch_runtime` legacy rifiutato; `__main__`/`lock`/`quote` → `REAL_PROVIDER_DISABLED`). | **REPRODUCED** → residuo `BLOCKED_PROVIDER_GATE` |
| **ORPHAN RESERVED LEASE** | Core `registry/reservations.py:reserve_or_get_live` (riga `RESERVED`, `submit_intent_at NULL`), `recover_orphaned_submits` (ignora righe senza intento), `due_for_reconcile` (le elenca, nessun consumatore); Core `transport/pipeline.py:resume_job` (RESERVED → restituita cosi' com'e'); Runtime `go_candidate.go` (`reserved-no-dispatch`; `new_attempt` → `LIVE_OR_UNCERTAIN_ATTEMPT`). | `reproduction_baseline.json["ORPHAN_RESERVED"]`: reservation committata senza `mark_submitting` → `resume` = `EXISTING_LIVE_JOB/reserved-no-dispatch` (0 submit), `new_attempt` stessa operazione = `LIVE_OR_UNCERTAIN_ATTEMPT`, `recover_orphaned_submits` = `[]`, `due_for_reconcile` la elenca, `reserved_units` resta impegnato. | Operazione bloccata per sempre; budget impegnato per sempre; nessuna authority di lease/timeout definita. | Nessun auto-dispatch, nessuna policy arbitraria. Si formalizzano stati e ownership (`ORPHAN_LEASE.md`), si aggiunge la **classificazione** read-only (`runtime/orphan_lease.py`) e si documenta la policy candidata con l'invariante del Core (nessun intento persistito ⇒ nessun invio) e l'analisi delle race. La **reclaim** richiede una lease authority (finestra, attore) che non esiste: decisione umana. | B08 (riproduzione deterministica + classificazione + zero blind retry). | **REPRODUCED** → resta **STILL_OPEN** |
| **AUTHORIZATION / PRICING AUTHORITY** | `runtime/authorization.py` (`DEFAULT_LAB_AUTHORIZATION`, `LAB_PRICE_TABLE`, `lab_price_for`); quote emesse da `tests/worker.py:store_call("issue_lab_quote")` e `tests/boundary_mock.py:worker_main` (op `quote`). | `reproduction_baseline.json["AUTHORIZATION_PRICING"]`: costanti LAB in codice; in modalita' same-UID l'orchestrator puo' emettere quote/envelope sullo store del worker. | Autorita' non separata dal client. | Hardening LAB: con P-B01 l'autorita' (envelope/quote) vive **solo** nel processo spender; l'orchestrator non puo' aprire lo store ne' emettere quote (verificato come probe). Nessun claim di "real provider authorization" o "real pricing verified". | B09 (autorita' irraggiungibile dall'orchestrator; importo dal client rifiutato; listino LAB dichiarato). | **LAB_ONLY** (verificato) — non reale |

Premesse del mandato verificate: nessuna premessa falsa trovata. Un'unica precisazione: P-B04 e' **parzialmente** coperto dal Core (RV02: digest autorizzato + trasporto che rifiuta digest non autorizzati) — cio' che manca ed e' riprodotto e' la persistenza exact-byte, la verifica su evidenza persistita immediatamente prima del send e l'audit; il fix e' quindi Runtime-only e non tocca RV02.
