# TEST_MATRIX — PROVIDER / EXECUTION BOUNDARY GATE 01

Runner: `pbgate/run_boundary_gate.py` · classificazione: `RUNTIME_INTEGRATION_GATE_01/tests/gate_report.py` (tri-state PASS | FAIL | BLOCKED,
BLOCKED solo via `GateBlocked` per precondizione ambientale; policy LAB: `B01`/`B09` possono essere `BLOCKED_ENVIRONMENT`) · inventario canonico B00..B13.
Processi reali (`multiprocessing` spawn) per ogni helper; UID distinti via `setpriv` per B01. Nessun provider reale, nessuna credenziale, 0 crediti.

| ID | Gap | Requisito | Riproduzione pre-fix | Controprove fail-closed | Evidenza |
|---|---|---|---|---|---|
| B00 | Reality Lock | Core HEAD `740ee979` pulito; Runtime `origin/main` `39c82968`; HEAD discende; pin; P2 `637f3a80` | — | — | `B00_reality_lock.json` |
| B10 | Regressione | `run_gate.py` T01-T37 sul runtime modificato: 36 PASS, T29 `BLOCKED_ENVIRONMENT`, 0 FAIL, inventario valido, T16/T17/T19 PASS; file storici ripristinati | — | — | `B10_regression_r0_r1.json`, `regression/*` |
| B01 | P-B01 | orchestrator uid≠spender uid, `CapEff=0`, `NoNewPrivs=1`; 4 probe canoniche T29 BLOCKED (`t29_status` distinct-uid → PASS); peer uid autenticato; terzo uid rifiutato; spesa governata via socket sul conto dello spender con snapshot `send_verified`; resume 0 submit; segreto mai nel canale; sentinella 0; store/segreto/ledger nel dominio dello spender | T29 storico e regressione (same-UID → 4× BYPASS_POSSIBLE → BLOCKED) | read secret, write code (3 file), write store, direct dispatch, issue quote, kill, /proc/environ, list secret dir, read ledger, peer uid estraneo, submit senza intent, amount dal client, shell | `B01_privilege_boundary_uid.json`, `raw/B01_spender_daemon.log` |
| B02 | P-B01 controllo | policy congelata: same-uid → BLOCKED qualunque probe; distinct-uid: 4/4 → PASS, 3/4 o 1 bypass → FAIL; T29 storico/regressione BLOCKED | — | 3/4 probe, 1 bypass | `B02_same_uid_control.json` |
| B03 | P-B02 | report valido → stato scoperto, `provider_job_id` riempito, evidenza `signature_verified` registrata; il job resta riconciliabile dopo i rifiuti | `reproduction_baseline.json["P-B02"]` (evidenza arbitraria accettata, riusata) | conto sbagliato (campo / chiave), provider sbagliato (report / adapter / adapter con propria chiave), job sconosciuto, token di altro job, token errato, digest errato, operazione errata, firma assente / chiave errata / campo alterato, stato non riferibile, report malformato — 16, journal invariato | `B03_reconciliation_authenticated.json` |
| B04 | P-B02 | replay → `REPLAY`; report valido di altro job → `JOB_MISMATCH`; RESERVED senza intento → `OWNERSHIP_INCOMPLETE`; bind snapshot rimosso → `SNAPSHOT_AUDIT_FAILED`; chiamanti runtime di `reconcile` determinati per **AST** = esattamente `reconciliation.py:reconcile_authenticated` e `go_candidate.py:_refuse_pre_submit` (i due percorsi con provenance dichiarata); percorso raw Core ancora permissivo (dichiarato) | idem | replay, cross-operation, audit | `B04_reconciliation_replay_cross_operation.json` |
| B05 | P-B04 | byte persistiti == canonici ricalcolati; sha256(file) == `payload_digest`; `0444`; seal/bind/send (4 file); resume 0 nuovi file; audit ok | `reproduction_baseline.json["P-B04"]` (0 byte su disco) | overwrite write-once, seal conflitto su spec diversa, altra operazione (`SEAL_MISSING`), token errato, secondo invio (`SEND_ALREADY_ATTESTED`), reorder (`SNAPSHOT_MISSING`), audit altra op | `B05_snapshot_exact_byte.json` |
| B06 | P-B04 | 6 controprove rifiutate PRIMA del marcatore (`sent_count 0`), `FAILED` settlement 0, anomalia registrata, nessun `SUBMIT_UNKNOWN`, nessun bind per drift; **provenance per percorso** (NG-03): drift a `mark_submitting` → `RUNTIME_PRE_SUBMIT_REFUSED_NOT_DISPATCHED`, rifiuti dentro `submit` → `TRANSPORT_ATTESTED_NOT_SENT`; i casi drift non affermano mai l'attestazione del trasporto; adapter corretto poi spende; pre-fix il caso "substitute" raggiungeva l'invio | idem (`drift_case`: `sent_count 1`, `SUBMIT_UNKNOWN`) | nested mutation, serialization drift, field reorder, payload sostituito dopo authorization, snapshot mancante, digest mismatch | `B06_snapshot_counterproofs.json` |
| B07 | Legacy | inventario statico (22 file) + dinamico: switch chiuso → `LEGACY_SPEND_PATH_DISABLED` prima di import Core e store; governato invariato; `hf_batch_runtime` legacy chiuso → 0 submit, aperto → marcato; adapter=None → `REAL_PROVIDER_DISABLED`; `__main__` go → `NO LOCK`, lock/quote → `REAL_PROVIDER_DISABLED`; P2 frozen non importato; sentinella 0 | `reproduction_baseline.json["LEGACY_SPEND_PATH"]` | switch, hf_batch, __main__ | `B07_legacy_spend_paths.json`, `LEGACY_SPEND_PATHS.md` |
| B08 | Orphan lease | `RESERVED_NO_INTENT` senza uscite; resume 0 submit; new_attempt rifiutato; recover ignora; `due_for_reconcile` elenca; reconcile → `OWNERSHIP_INCOMPLETE`; crash dopo invio → `RESERVED_WITH_INTENT` → recover `SUBMIT_UNKNOWN`; reclaim `NOT_AUTHORIZED` | `reproduction_baseline.json["ORPHAN_RESERVED"]` | — (gap resta STILL_OPEN) | `B08_orphan_reserved_lease.json`, `ORPHAN_LEASE.md` |
| B09 | Authority | listino/envelope LAB dichiarati; orchestrator non emette quote ne' scrive lo store spender; importo dal client → PROTOCOL; real: NOT_VERIFIED | — | — | `B09_authority_lab_state.json` |
| B12 | NG-03 (delta correttivo) | provenance del settlement 0 corrispondente all'evidenza prodotta: rifiuto a `mark_submitting` → `FAILED` + `SETTLE 0` con `RUNTIME_PRE_SUBMIT_REFUSED_NOT_DISPATCHED` e fatti osservati; rifiuto dentro `submit` → `TRANSPORT_ATTESTED_NOT_SENT` invariato; invio incerto → `SUBMIT_UNKNOWN` senza settlement; race → `StaleWrite`, un solo `SETTLE`, `settle` idempotente, importo diverso → `SettlementConflict` | `evidence/pre_fix/reproduction_ng03.json` (A e B registravano la stessa provenance) | 5 controprove obbligatorie del review | `B12_pre_submit_provenance.json`, `CORRECTIVE_DELTA_NG03.md` |
| B13 | Reporting (delta correttivo HR-02) | test-suite result e phase readiness separati e coerenti: `all_tests_passed` vs `all_requirements_verified`; requisiti aperti elencati con stato; `ALL_VERIFIED` solo senza aperti; report forgiato rifiutato da `validate()`; console/JSON/Markdown/MANIFEST allineati | `RESULTS.json` v2 dichiarava insieme ALL_VERIFIED e requisiti aperti | 5 controprove del review + report forgiato + verifiche sul report reale | `B13_reporting_phase_readiness.json`, `CORRECTIVE_DELTA_REPORTING.md` |
| B11 | Freeze | P2 before == after == canonico (anche `LAB/hf_batch_ORIGINAL.py`); Core HEAD/tree invariati; static checks 0 findings | — | — | `B11_p2_freeze_core_pin_after.json` |
